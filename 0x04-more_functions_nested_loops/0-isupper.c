#include "main.h"
/**
 * _isupper - prints uppercase letter
 * @c: character to be printed
 * Return: 0
 */
int _isupper(int c)
{
	if ((c >= 'A') && (c <= 'Z'))
		return (1);
	else
		return (0);
	return (0);
}
